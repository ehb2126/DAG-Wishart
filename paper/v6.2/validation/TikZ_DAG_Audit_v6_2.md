# TikZ DAG audit for working preprint v6.2

Every graph-structured figure in the paper is now generated by TikZ inside the
main LaTeX file.  The source contains no `\includegraphics` call for a DAG.

| Figure | Embedded graph | Audit result |
|---|---|---|
| 1 | Complete-DAG schematic | Eight displayed vertices and all 28 directed pairs, ordered from higher to lower labels. |
| 2 | Four-node cycle skeleton | Exact edges `2→1`, `3→1`, `4→2`, `4→3`. |
| 3 | Collider example | Exact edges `3→1`, `2→1`. |
| 4a | Molecular benchmark | 11 vertices and 19 benchmark edges. |
| 4b | LassoDAG estimate | 15 benchmark-agreeing edges in blue and 17 additional edges in dashed red. |
| 4c | DAG-W estimate | 18 benchmark-agreeing edges in blue and 19 additional edges in dashed red. |
| 5 | Hausdorff collider example | Reuses the same exact `3→1←2` TikZ macro as Figure 3. |

The molecular edge lists were recovered directly from the previous vector PDF,
including arrow directions and color classes, before the external artwork was
removed.  The node positions follow the prior circular layout.

The only remaining raster/vector inclusions are `Explanation.pdf` and
`CallCenterError-2002-Final.pdf`; both are numerical plots rather than DAGs.

